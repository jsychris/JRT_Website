import {formatCost} from '@/lib/event-options';
import { ArrowUpRight, CalendarDays, MapPin } from 'lucide-react';
export function PageHead({eyebrow,title,children}:{eyebrow:string,title:string,children?:React.ReactNode}){return <section className="pagehead"><div className="wrap"><div className="breadcrumb"><a href="/">Home</a> / {eyebrow}</div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1>{children&&<p>{children}</p>}</div></section>}
export function JoinCTA(){return <div className="cta"><h2>Your next good story<br/>starts with a Tuesday.</h2><a className="btn dark" href="/join">Come along and meet us <ArrowUpRight size={19}/></a></div>}
export function EventRow({event,member=false}:{event:any,member?:boolean}){const d=new Date(event.starts_at);return <article className="eventrow"><div className="datebox">{d.toLocaleDateString('en-GB',{month:'short',timeZone:'Europe/Jersey'})}<strong>{d.toLocaleDateString('en-GB',{day:'2-digit',timeZone:'Europe/Jersey'})}</strong>{d.getFullYear()}</div><div><span className="pill">{event.category}</span>{event.status==='cancelled'&&<span className="pill">Cancelled</span>}<h3>{event.title}</h3><div className="eventmeta"><span><CalendarDays size={15}/>{d.toLocaleString('en-GB',{weekday:'short',hour:'2-digit',minute:'2-digit',timeZone:'Europe/Jersey'})}</span><span><MapPin size={15}/>{event.location||'Location to follow'}</span><span>{formatCost(event.cost_pence||0)}{event.cost_pence>0?' per person':''}</span></div></div><a className="btn outline small" href={member?`/members?event=${event.id}`:`/events/${event.id}`}>View event <ArrowUpRight size={16}/></a></article>}
